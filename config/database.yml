# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
# Reemplazado por Paulo Perez sqlite3

  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling

development:
  database: dbblog_dev
  adapter: postgresql
  pool: 5
  timeout: 5000

test:
  database: dbblog_test
  adapter: postgresql
  pool: 5
  timeout: 5000

production:
  database: dbblog_prod
  adapter: postgresql
  pool: 5
  timeout: 5000
